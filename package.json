{
  "name": "outside-workspace",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "turbo run build",
    "build-nodaemon": "turbo run build --no-daemon",
    "clean": "rm -rf ./dist && rm -rf ./packages/example-0/dist && rm -rf ./packages/example-2/dist",
    "clean-cache": "rm -rf node_modules/.cache/turbo"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "turbo": "canary" 
  },
  "workspaces": [
    "packages/**"
  ]
}
